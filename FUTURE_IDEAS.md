# Future Ideas (not in v1 — parked on purpose)

Ideas go here instead of into the code, until the current build is shipped and working end to end.

- Self-host Baloo 2 / Nunito as local woff2 files (currently falls back to system rounded fonts —
  looks good, but the original hand-picked type is even better)
- Hold/swap piece slot
- Optional "marathon vs. sprint" mode select
- Share-score-as-image (canvas → downloadable PNG) instead of the current disabled share button
- Real app icon pass — current icon is a placeholder mascot-blob generated programmatically;
  worth a proper design pass once this ships
- Apple App Store build via Capacitor, once there's budget for a developer account
